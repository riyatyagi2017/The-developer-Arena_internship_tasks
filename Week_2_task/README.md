## Create Python file 
*Create your python file named as grade_calculator.py*

## Write function to calculate_grade
 - Write a function to calulate grade and generate encouraging message for each grade.
 - use if-elif-else inside calculate_grade() function.

## Write a function get_valid_marks
 - Write a function to get_valid_marks().
 - In function use While loop for continuous validation.
 - Inside While loop use try and except block to check if marks are between 0 and 100.
 - try-except block will also handle non-numeric input.

## define main program
 - Inside main function get name and marks using Input() function.
 - Combine all parts using above define function.

# Run Progrram.
 - main()
 - Program ask for user input,
    - Enter student name:
    - Enter marks(0-100):
- Then generate the result according to the build logic
- Give the name, marks, grade and generate encouraging message for that grade.